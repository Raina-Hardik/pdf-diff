from __future__ import annotations

from pathlib import Path


def diff_markdown_files(left: Path, right: Path) -> str:
    """TODO: implement Markdown diff support behind the optional delta dependency group."""
    raise NotImplementedError(
        "Markdown diff is not implemented yet. Install support later via pdf-diff[delta]."
    )
